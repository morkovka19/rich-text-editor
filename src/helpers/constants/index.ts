import { TAGS } from '../../types';

export const MAIN_DIV_ID = 'root-div';
export const EDITOR_ID = 'editor';

export const mapForAddedNodes = new Map<string, string>([[TAGS.BLOCK, TAGS.PARAGRAPH]]);
